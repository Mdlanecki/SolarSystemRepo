Renders a model solar system in WebGL

Start program with "python -m http.server 8000" within console, access at local host in browser.

Writen in Javascript, uses Web2GL API. Fragment and Vertex shaders included.
